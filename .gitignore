# Julia depot kept inside the project by shell.nix
/.julia_depot/
/.julia_versions/

# Julia
/Manifest.toml
/wasm/Manifest.toml
*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/

# Pluto notebook autosave / backups
*.jl.backup
.pluto_states/

# Editor / OS
.DS_Store
.vscode/
*.swp

# Model artifacts / cached posterior draws
/output/
*.jls
*.arrow

# Local Julia 1.12 install used for Snapshot / WasmTarget builds
# (CI deploys the committed site/ directory to GitHub Pages)
